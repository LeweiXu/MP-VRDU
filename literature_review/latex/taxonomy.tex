% ─────────────────────────────────────────────────────────────────────────────
% MP-VRDU Literature Review — Taxonomy Figure
%
% Required packages (in acl_latex.tex preamble):
%   \usepackage{tikz}
%   \usepackage{forest}
%   \usepackage{xcolor}
%   \usepackage{adjustbox}
%   \usetikzlibrary{arrows.meta}
% ─────────────────────────────────────────────────────────────────────────────

% ── Column widths — edit these to resize boxes per column. ───────────────────
% If the text exceeds the width, it will wrap onto a second (or third) line.
\newcommand{\TaxRootWidth}{30mm}
\newcommand{\TaxSectionWidth}{17mm}
\newcommand{\TaxSubsectionWidth}{28mm}
\newcommand{\TaxIdeaWidth}{26mm}
\newcommand{\TaxLeafWidth}{63mm}

% ── Spacing parameters. ──────────────────────────────────────────────────────
% \TaxEdgeStub: length of the horizontal stub leaving each parent box before
% the edge turns vertical. Increase if the perpendicular gap looks too small.
\newcommand{\TaxLevelSep}{14pt}
\newcommand{\TaxSiblingSep}{1.2pt}
\newcommand{\TaxEdgeStub}{6pt}

% ── Colours — edit to recolour the figure. ───────────────────────────────────
\definecolor{nodebg}{HTML}{E8E8E8}
\definecolor{leafbg}{HTML}{D6E6F4}

\begin{figure*}[p]
\centering
\begin{adjustbox}{max totalsize={\textwidth}{0.92\textheight},center}
\begin{forest}
  for tree={
    grow'=east,
    parent anchor=east,
    child anchor=west,
    anchor=west,
    align=left,
    edge={-{Stealth[length=3pt]}, thin, gray!55},
    edge path={
      \noexpand\path[\forestoption{edge}]
        (!u.parent anchor) -- +(\TaxEdgeStub,0) |- (.child anchor)\forestoption{edge label};
    },
    l sep=\TaxLevelSep,
    s sep=\TaxSiblingSep,
    font=\scriptsize,
    rounded corners=2pt,
    draw=gray!50,
    inner sep=2.5pt,
    inner xsep=4pt,
    fill=nodebg,
  },
  % Root: rotated 90° banner. parent anchor=south puts the outgoing edge on
  % the visible east of the rotated box, so the line does not overlap the box.
  where level=0{
    font=\small\bfseries,
    align=center,
    rotate=90,
    anchor=center,
    parent anchor=south,
    text width=\TaxRootWidth,
  }{},
  % Section column.
  where level=1{font=\scriptsize\bfseries, text width=\TaxSectionWidth, align=left}{},
  % Subsection column.
  where level=2{text width=\TaxSubsectionWidth, align=left}{},
  % Idea column (level 3 non-leaf; level-3 leaves in §7/§8 fall through to leaf rule below).
  where level=3{text width=\TaxIdeaWidth, align=left}{},
  % Leaf rule comes LAST so it overrides any level-specific width above.
  % Applies to every node with no children (level 4 in §3–§6, level 3 in §7/§8).
  where n children=0{
    tier=leafrow,
    fill=leafbg,
    font=\tiny,
    text width=\TaxLeafWidth,
    align=left,
  }{},
  [MP-VRDU Taxonomy
    %% ── §3 Architecture ───────────────────────────────────────────────────
    [§\ref{sec:architecture}~Architecture
      [§\ref{sec:arch-p2d}~Page-to-Document
        [Summary tokens
          [{Hi-VT5~\cite{tito2023hierarchical}}]
        ]
        [Cross-page attention
          [{Arctic-TILT~\cite{borchmann2025arctictilt}, GRAM~\cite{blau2024gram}}]
        ]
        [Recurrent state
          [{RM-T5~\cite{dong2024multipage}}]
        ]
      ]
      [§\ref{sec:arch-bc}~Backbone-Centric
        [Lightweight projector
          [{InstructDoc~\cite{tanaka2024instructdoc}}]
        ]
        [Token compression
          [{mPLUG-DocOwl2~\cite{hu2025mplugdocowl2}}]
        ]
        [Backbone fine-tuning
          [{Docopilot~\cite{duan2025docopilot}, DocR1~\cite{xiong2026docr1}}]
        ]
      ]
      [§\ref{sec:arch-rg}~Retriever-Generator
        [Single retriever-reader
          [{M3DocRAG~\cite{cho2024m3docrag}, VDocRAG~\cite{tanaka2025vdocrag}}]
        ]
        [Multi-component pipeline
          [{MDocAgent~\cite{han2025mdocagent}, M2RAG~\cite{duan2025m2rag}}]
        ]
      ]
      [§\ref{sec:arch-agent}~Agentic
        [Single-controller
          [{Doc-V*~\cite{zheng2026docv}, DocAgent~\cite{sun2025docagent}}]
        ]
        [Multi-agent
          [{MACT~\cite{yu2025mact}, ViDoRAG~\cite{wang2025vidorag}}]
        ]
      ]
    ]
    %% ── §4 Modalities ────────────────────────────────────────────────────
    [§\ref{sec:modality}~Modalities
      [§\ref{sec:modality-text}~Text
        [Per-page aggregation
          [{Hi-VT5~\cite{tito2023hierarchical}, GRAM~\cite{blau2024gram}}]
        ]
        [Whole-doc compression
          [{mPLUG-DocOwl2~\cite{hu2025mplugdocowl2}, TextHawk2~\cite{yu2024texthawk2}}]
        ]
        [Retrieve-then-read
          [{CREAM~\cite{zhang2024cream}, MultiDocFusion~\cite{shin2025multidocfusion}}]
        ]
      ]
      [§\ref{sec:modality-visual}~Visual
        [Token compression
          [{mPLUG-DocOwl2~\cite{hu2025mplugdocowl2}, TextHawk2~\cite{yu2024texthawk2}}]
        ]
        [Selective high-res
          [{Doc-V*~\cite{zheng2026docv}, SimpleDoc~\cite{jain2025simpledoc}}]
        ]
        [Retrieval-only
          [{M3DocRAG~\cite{cho2024m3docrag}, VDocRAG~\cite{tanaka2025vdocrag}}]
        ]
      ]
      [§\ref{sec:modality-structure}~Structure
        [Implicit recovery
          [{mPLUG-DocOwl2~\cite{hu2025mplugdocowl2}, TextHawk2~\cite{yu2024texthawk2}}]
        ]
        [Explicit parsed
          [{MultiDocFusion~\cite{shin2025multidocfusion}, KGP~\cite{wang2024knowledge}}]
        ]
      ]
    ]
    %% ── §5 Inference-Time Adaptation ─────────────────────────────────────
    [§\ref{sec:inference-time}~Inference
      [§\ref{sec:tf-retrieval}~Retrieval \& Navigation
        [Similarity-based
          [{M3DocRAG~\cite{cho2024m3docrag}, SimpleDoc~\cite{jain2025simpledoc}}]
        ]
        [Structure-based
          [{MultiDocFusion~\cite{shin2025multidocfusion}, KGP~\cite{wang2024knowledge}}]
        ]
      ]
      [§\ref{sec:tf-reasoning}~Reasoning
        [Closed-buffer (CoT)
          [{MHier-RAG~\cite{gong2025mhierrag}, SimpleDoc~\cite{jain2025simpledoc}}]
        ]
        [Tool-interactive (ReAct)
          [{Doc-React~\cite{wu2025docreact}}]
        ]
      ]
      [§\ref{sec:agent-orchestration}~Orchestration
        [Fixed-trajectory
          [{MDocAgent~\cite{han2025mdocagent}, M2RAG~\cite{duan2025m2rag}}]
        ]
        [Adaptive loop
          [{ViDoRAG~\cite{wang2025vidorag}}]
        ]
        [Proposer-verifier
          [{DocLens~\cite{zhu2025doclens}, DocAgent~\cite{sun2025docagent}}]
        ]
      ]
    ]
    %% ── §6 Training Strategies ───────────────────────────────────────────
    [§\ref{sec:training}~Training
      [§\ref{sec:train-retrieval}~Retrieval
        [Modality alignment
          [{VDocRAG~\cite{tanaka2025vdocrag}}]
        ]
        [Cross-page fusion
          [{DFVC~\cite{qian2026accurate}}]
        ]
        [Logical relevance
          [{MoLoRAG~\cite{wu2025molorag}}]
        ]
      ]
      [§\ref{sec:train-reasoning}~Reasoning
        [Reward-driven RL
          [{DocR1~\cite{xiong2026docr1}}]
        ]
        [Trace imitation
          [{CoR~\cite{guo2026endtoend}}]
        ]
        [Uncertainty-driven
          [{DocSLM~\cite{hannan2025docslm}}]
        ]
      ]
      [§\ref{sec:train-orchestration}~Orchestration
        [RL / behavioural cloning
          [{DocDancer~\cite{zhang2026docdancer}, Doc-V*~\cite{zheng2026docv}, MACT~\cite{yu2025mact}}]
        ]
      ]
      [§\ref{sec:train-crosscutting}~Cross-Cutting
        [PEFT
          [{LayTokenLLM~\cite{zhu2025laytokenllm}, CREAM~\cite{zhang2024cream}}]
        ]
        [Multi-stage curriculum
          [{mPLUG-DocOwl2~\cite{hu2025mplugdocowl2}, DocSLM~\cite{hannan2025docslm}}]
        ]
      ]
    ]
    %% ── §7 Datasets ──────────────────────────────────────────────────────
    [§\ref{sec:datasets}~Datasets
      [§\ref{sec:datasets}~Synthetic generation
        [{InstructDoc~\cite{tanaka2024instructdoc}, DocDancer~\cite{zhang2026docdancer}}]
      ]
      [§\ref{sec:datasets}~Long-document
        [{MMLongBench-Doc~\cite{ma2024mmlongbenchdoc}, LongDocURL~\cite{deng2025longdocurl}}]
      ]
      [§\ref{sec:datasets}~Recurring benchmarks
        [{MP-DocVQA~\cite{tito2023hierarchical}, DUDE~\cite{vanlandeghem2023document}}]
      ]
    ]
    %% ── §8 Open Gaps ─────────────────────────────────────────────────────
    [§\ref{sec:gaps}~Open Gaps
      [§\ref{sec:gap-data}~Data \& Eval Integrity
        [{InstructDoc~\cite{tanaka2024instructdoc}, DocDancer~\cite{zhang2026docdancer}}]
      ]
      [§\ref{sec:gap-evidence}~Evidence Pipeline
        [{MoLoRAG~\cite{wu2025molorag}, KGP~\cite{wang2024knowledge}}]
      ]
      [§\ref{sec:gap-structure}~Cross-Page Structure
        [{MultiDocFusion~\cite{shin2025multidocfusion}, KGP~\cite{wang2024knowledge}}]
      ]
    ]
  ]
\end{forest}
\end{adjustbox}
\caption{Taxonomy of MP-VRDU systems organised by architectural family (\S\ref{sec:architecture}), modality handling (\S\ref{sec:modality}), inference-time strategy (\S\ref{sec:inference-time}), training strategy (\S\ref{sec:training}), datasets (\S\ref{sec:datasets}), and open gaps (\S\ref{sec:gaps}). Each leaf names representative systems with citations.}
\label{fig:taxonomy}
\end{figure*}
